#foreach ($rectangle in $myRectangles)
  <h3>Your rectangle's length is $rectangle.getLength()</h3>
  <h3>Your rectangle's width is $rectangle.getWidth()</h3>
  #if ($rectangle.isSquare())
    <h3>Your rectangle is a square!</h3>
  #else
    <h3>Your rectangle is not a square!</h3>
  #end
#end
